// Getting necessary dependancies

const express = require('express')
const path = require('path');
const fs = require('fs');

// Create Server

const app = express;
const port = process.env.PORT || 3000;